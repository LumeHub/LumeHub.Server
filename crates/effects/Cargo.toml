[package]
name = "effects"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
engine = { path = "../engine" }
application = { path = "../application" }
rhai = { version = "1.24.0", features = ["sync"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
config = "0.15.18"
include_dir = "0.7"
