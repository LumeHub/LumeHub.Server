[signals]
meteor3 = {r=0, g=0, b=0}
meteor4 = {r=0, g=0, b=0}

[[layers]]
effect = "script"
speed1 = 0.6
speed2 = 0.35
speed3 = 0.5
speed4 = 0.8
tail1  = 18.0
tail2  = 30.0
tail3  = 22.0
tail4  = 14.0
code   = """
let f1 = meteor_factor(pixel, len, time, speed1, tail1,   0.0);
let f2 = meteor_factor(pixel, len, time, speed2, tail2,  50.0);
let f3 = meteor_factor(pixel, len, time, speed3, tail3, 100.0);
let f4 = meteor_factor(pixel, len, time, speed4, tail4, 150.0);
add_colors(
    add_colors(dim(primary_color,   f1), dim(secondary_color, f2)),
    add_colors(dim(meteor3,         f3), dim(meteor4,         f4))
)
"""
